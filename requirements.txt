# MCPWatch runtime dependencies. Python 3.11+.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
python-dotenv>=1.0

# Production database (Postgres/Supabase). SQLite (stdlib) is used when DATABASE_URL is unset,
# so these are only needed for a Postgres deployment.
psycopg[binary]>=3.1
psycopg_pool>=3.2

# The grading engine. Until mcp-probe is published to PyPI, install it editable and/or set
# MCP_PROBE_PATH to its source directory:
#   pip install -e ../Building/mcp-probe
# mcp-probe
